# ⚡ Dashboard - Быстрая проверка

Краткий чек-лист для acceptance testing.

---

## 🚀 Запуск

```bash
npm start
# → http://localhost:4200 (auto redirect → /dashboard)
```

---

## ✅ Что проверить по ролям

### 👨‍🏫 TEACHER
```
Login: создай user с ролью TEACHER

✓ Видит:
  - + Attendance sheet (зелёная кнопка)
  - Блок "Сегодня" (виджет с уроками)

✗ НЕ видит:
  - + Create user
  - + Add lesson
```

### 📅 ADMIN_SCHEDULE
```
Login: создай user с ролью ADMIN_SCHEDULE

✓ Видит:
  - + Add lesson (фиолетовая кнопка)

✗ НЕ видит:
  - + Create user
  - + Attendance sheet
  - Блок "Сегодня"
```

### 👨‍💼 ADMIN
```
Login: admin@local / admin123

✓ Видит:
  - + Create user (синяя кнопка)
  - + Attendance sheet (зелёная кнопка)

✗ НЕ видит:
  - + Add lesson (если нет роли ADMIN_SCHEDULE)
  - Блок "Сегодня" (если не преподаёт)
```

### 🦸 SUPER_ADMIN
```
Login: создай user с ролью SUPER_ADMIN

✓ Видит ВСЁ:
  - + Create user
  - + Add lesson
  - + Attendance sheet
  - Блок "Сегодня" (если есть teacherId)
```

---

## 📅 Виджет "Сегодня" (TEACHER)

### Если backend готов:
```
GET /api/timetable/teacher/{userId}/today
→ Показывает до 5 уроков, отсортированных по времени

Формат:
09:00–10:30
Subject math-101 — Group 10-A
Room 205
```

### Если backend НЕ готов:
```
→ Показывает: "Нет занятий на сегодня"
→ НЕ показывает красный snackbar
→ Ошибка только в console.log
```

---

## 🔄 Interceptors

### Loading:
```
✓ Spinner появляется во время загрузки
✓ Spinner исчезает после завершения
→ Смотри верхний правый угол
```

### Auth:
```
✓ Bearer token добавляется автоматически
→ Проверь DevTools → Network → Request Headers
```

### Error:
```
✓ Сетевые ошибки → красный snackbar
✓ 404 от Today's Lessons → НЕ показывает snackbar
```

---

## 🖱️ Навигация кнопок

| Кнопка | Ведёт на | Может быть 404? |
|--------|----------|-----------------|
| + Create user | `/users` | Нет (страница готова) |
| + Add lesson | `/timetable` | Да (страница не создана) |
| + Attendance sheet | `/attendance` | Да (страница не создана) |

**404 — это нормально!** Просто страницы ещё не реализованы.

---

## 📱 Responsive

### Desktop:
```
✓ 2 колонки для виджетов
✓ Quick Actions на всю ширину
```

### Mobile:
```
✓ 1 колонка
✓ Кнопки переносятся
```

---

## 🐛 Debug Commands

### Проверить роли:
```javascript
const token = sessionStorage.getItem('aq_access');
const payload = JSON.parse(atob(token.split('.')[1]));
console.log(payload.roles);
// Expected: ["TEACHER"] or ["ADMIN"] etc.
```

### Проверить weekday:
```javascript
console.log('Today:', new Date().getDay());
// 0=Sun, 1=Mon, 2=Tue, 3=Wed, 4=Thu, 5=Fri, 6=Sat
```

### Убрать spinner если завис:
```javascript
inject(LoadingService).forceHide();
```

---

## ✅ Acceptance Criteria

Dashboard готов если:

- [x] Разные роли → разные кнопки
- [x] TEACHER видит "Сегодня"
- [x] Уроки фильтруются по weekday
- [x] Loading spinner работает
- [x] Навигация работает
- [x] Responsive работает
- [x] Ошибки показывают snackbar (кроме 404 в Today's Lessons)

---

**Полная документация:** `DASHBOARD_ACCEPTANCE.md`
